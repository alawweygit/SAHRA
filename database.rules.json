{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": "auth == null || auth != null",
        "state": { ".write": true },
        "mirror": { ".write": true },
        "players": {
          "$pid": {
            ".write": true,
            ".validate": "!newData.exists() || (newData.hasChildren(['name','emoji','color','score','isVip','joinedAt']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 14 && newData.child('emoji').isString() && newData.child('color').isString() && newData.child('score').isNumber() && newData.child('isVip').isBoolean() && newData.child('joinedAt').isNumber())"
          }
        },
        "inputs": {
          "$phaseId": {
            "$pid": { ".write": true }
          }
        }
      }
    }
  }
}
